import { Text, View } from "react-native";

// Unfinished profile page, currently just a placeholder that says "Profile page"
export default function Index() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Profile page</Text>
    </View>
  );
}
